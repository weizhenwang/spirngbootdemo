spring:
  profiles:
    active: a
  datasource:
      data-source: com.mysql.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/db_person?useUnicode=true&characterEncoding=UTF-8
      username: root
      password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true